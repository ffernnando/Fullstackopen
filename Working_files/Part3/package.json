{
  "name": "backend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "build:ui": "rmdir /s /q dist && cd ../Part2/examples_2a && npm install && npm run build && xcopy dist ../../Part3/dist /E /I /Y",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Matti Luukkainen",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "express": "^5.1.0"
  }
}
